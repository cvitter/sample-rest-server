pipeline {
    agent any

	stages {
		stage('Checkout') {
			steps {
				echo 'Stage: Checkout'
				git 'https://github.com/cvitter/sample-rest-server'
			}
		}
		stage('Build') {
			steps {
				echo 'Stage: Build'
				
				
			}
		}
    }
}